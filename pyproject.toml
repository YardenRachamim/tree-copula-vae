[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tree-copula-vae"
version = "0.1.0"
description = "Scaffold for the tree-copula VAE project"
readme = "README.md"
requires-python = "==3.8"
